# Port for the notification service (default: 9000)
PORT=9000

# VAPID keys for Web Push — leave blank to auto-generate on first run.
# The auto-generated keys will be printed to stdout; copy them here to persist.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
